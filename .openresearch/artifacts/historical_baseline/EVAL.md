# Historical rejected baseline

Expected result: all six historical sanity checks execute, while every result
remains explicitly labeled `TOY` or `PROXY`.

This is not the current verifier once a claim-faithful child is promoted.
