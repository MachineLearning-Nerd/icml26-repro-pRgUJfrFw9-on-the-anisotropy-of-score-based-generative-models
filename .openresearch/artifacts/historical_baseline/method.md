# Historical rejected baseline

This baseline reconstructs the six NumPy sanity checks embedded in the exact
judged Space revision. It deliberately preserves their toy/proxy status and is
used only as a cumulative regression control.

Fixed command:

```bash
uv sync --locked && .venv/bin/python repro/run_campaign.py
```

The scientific campaign begins on child experiments. No PASS emitted here is a
VERIFIED paper claim.
