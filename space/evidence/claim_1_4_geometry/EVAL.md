# Claims 1/4 equal-stratum geometry calibration

Status: calibration retained, **not accepted for generation**.

The 8,000-network exact-architecture estimator passes every matrix audit, but
independent split-half vector overlap is only `0.657` at the largest
eigenvalue and `0.877` at the smallest. It therefore does not support a
generation-preference verdict. See `raw_geometry.json`.
