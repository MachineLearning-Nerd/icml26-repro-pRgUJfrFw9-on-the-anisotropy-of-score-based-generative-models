# Claims 1/4 CPU calibration

Planning evidence only; Claims 1 and 4 remain BLOCKED.

At author-exact `D=256`, the measured per-seed paper-scale projection is
`2.010 h` for 2,000 training steps plus `2.510 h` for 10,000 samples with
1,000 reverse steps. The paper's one-million-network geometry estimator
projects to `6.191 h`. See `raw_profile.json`.
